# Use the container/host clock as the authoritative source.
local stratum 8
manual

# Persist drift measurements.
driftfile /var/lib/chrony/drift
makestep 1.0 3

# Serve NTP to the network.
allow 0.0.0.0/0
allow ::/0

# Record measurements for debugging.
logdir /var/log/chrony
